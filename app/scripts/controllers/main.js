'use strict';

/**
 * @ngdoc function
 * @name allosoinsWebClientApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the allosoinsWebClientApp
 */
angular.module('allosoinsWebClientApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
